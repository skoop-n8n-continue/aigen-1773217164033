// Simple script for digital signage Hello World
document.addEventListener('DOMContentLoaded', () => {
    console.log('Hello World app initialized');

    // In a digital signage context, we might want to periodically refresh
    // or trigger animations, but CSS handles the continuous pulse animation here.
});
